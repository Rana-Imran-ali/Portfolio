@props(['label', 'title'])

<div class="mb-16">
    <span class="text-xs font-bold uppercase tracking-widest text-gray-400 block mb-2">{{ $label }}</span>
    <h2 class="text-4xl font-bold tracking-tighter">{{ $title }}</h2>
    <div class="mt-4 w-12 h-0.5 bg-black"></div>
</div>
